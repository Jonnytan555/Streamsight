[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "logger"
version = "0.1.1"
requires-python = ">=3.9"
dependencies = [
"concurrent-log-handler==0.*"
]

[tool.hatch.build.targets.wheel]
packages = ["logger"]
