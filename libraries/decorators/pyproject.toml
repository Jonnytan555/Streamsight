[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "decorators"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
"concurrent-log-handler==0.*"
]

[tool.hatch.build.targets.wheel]
packages = ["decorators"]
